apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
# Note: secret.yaml is a template only - create secrets manually:
#   kubectl create secret generic claude-runner-secrets \
#     --namespace claude-runner \
#     --from-literal=ANTHROPIC_API_KEY=<key> \
#     --from-literal=GITHUB_TOKEN=<token>
