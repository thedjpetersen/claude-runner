# Template only - NOT included in kustomization.yaml
# Create this secret manually before deploying:
#
#   kubectl create secret generic claude-runner-secrets \
#     --namespace claude-runner \
#     --from-literal=ANTHROPIC_API_KEY=<your-anthropic-api-key> \
#     --from-literal=GITHUB_TOKEN=<your-github-pat>
#
apiVersion: v1
kind: Secret
metadata:
  name: claude-runner-secrets
  namespace: claude-runner
type: Opaque
data:
  ANTHROPIC_API_KEY: <base64-encoded-key>
  GITHUB_TOKEN: <base64-encoded-token>
